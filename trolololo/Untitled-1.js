// stała rarr przypsiujemy do kalsy .right z pobr. z dokumenty HTML
const rArr = document.querySelector('right')
rArr.addEventListener('click', function () {
    const activeElement = activeElement.nextElementSibling
    if (!nextActiveElement)
}